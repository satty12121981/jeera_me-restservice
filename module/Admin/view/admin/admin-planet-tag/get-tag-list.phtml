<?php 
	if(isset($error)&&!empty($error)){
		echo "<ul>";
		foreach($error as $list){
			echo "<li>".$list."</li>";
		}
		echo "</ul>";
	}else{
?>		
	<?php 
		$i=0;
	foreach($tags as $tag_list){$i++;
		?>
		<a id="tag_list_<?php echo $tag_list->tag_id; ?>" href="javascript:void(0)" class="tag_list"><?php echo $tag_list->tag_title; ?></a>
	<?php 
	}
	if($i == 0){
		echo "No more tags are available";
	}else{
		echo '<a href="javascript:void(0)" id="tags_loadmore" class="tags_loadmore">Load more </a>';
	}
		?>
<?php 	} ?>
